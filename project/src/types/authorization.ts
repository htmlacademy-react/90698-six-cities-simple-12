export type AuthInfo = {
    email: string;
    password: string;
  };

export type UserInfo = {
    avatarUrl: string;
    email: string;
    id: number;
    isPro: boolean;
    name: string;
    token: string;
  };
